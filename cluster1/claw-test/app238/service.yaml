apiVersion: v1
kind: Service
metadata:
  name: perf-test7-app238-svc
  namespace: clawtest
  labels:
    app: perf-test7-app238
spec:
  selector:
    app: perf-test7-app238
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
