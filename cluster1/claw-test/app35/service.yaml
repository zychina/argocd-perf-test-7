apiVersion: v1
kind: Service
metadata:
  name: perf-test7-app35-svc
  namespace: clawtest
  labels:
    app: perf-test7-app35
spec:
  selector:
    app: perf-test7-app35
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
