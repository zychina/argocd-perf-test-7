apiVersion: v1
kind: Service
metadata:
  name: perf-test7-app404-svc
  namespace: clawtest
  labels:
    app: perf-test7-app404
spec:
  selector:
    app: perf-test7-app404
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
