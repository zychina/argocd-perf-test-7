apiVersion: v1
kind: Service
metadata:
  name: perf-test7-app662-svc
  namespace: clawtest
  labels:
    app: perf-test7-app662
spec:
  selector:
    app: perf-test7-app662
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
