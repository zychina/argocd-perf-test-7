apiVersion: v1
kind: Service
metadata:
  name: perf-test7-app762-svc
  namespace: clawtest
  labels:
    app: perf-test7-app762
spec:
  selector:
    app: perf-test7-app762
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
