apiVersion: v1
kind: Service
metadata:
  name: perf-test7-app855-svc
  namespace: clawtest
  labels:
    app: perf-test7-app855
spec:
  selector:
    app: perf-test7-app855
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
