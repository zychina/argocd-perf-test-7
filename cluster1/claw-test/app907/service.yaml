apiVersion: v1
kind: Service
metadata:
  name: perf-test7-app907-svc
  namespace: clawtest
  labels:
    app: perf-test7-app907
spec:
  selector:
    app: perf-test7-app907
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
