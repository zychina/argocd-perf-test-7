apiVersion: v1
kind: Service
metadata:
  name: perf-test7-app908-svc
  namespace: clawtest
  labels:
    app: perf-test7-app908
spec:
  selector:
    app: perf-test7-app908
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
