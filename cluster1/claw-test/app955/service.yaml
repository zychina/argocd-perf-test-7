apiVersion: v1
kind: Service
metadata:
  name: perf-test7-app955-svc
  namespace: clawtest
  labels:
    app: perf-test7-app955
spec:
  selector:
    app: perf-test7-app955
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
